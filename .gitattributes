# Git 병합 시 제외할 파일 설정
# merge=ours: 병합 시 현재 브랜치의 버전을 유지 (다른 브랜치의 변경사항 무시)

# 예시: dev 브랜치에서 병합 시 제외할 파일들
이게보인다면_dev입니다.txt merge=ours
release_sample.js merge=ours

# VERSION.txt는 병합 시 제외 (각 브랜치마다 다른 버전 유지)
VERSION.txt merge=ours
SUBMODULE_VERSION.txt merge=ours

# 개발 환경 설정 파일들 (dev 브랜치 전용)
# .env.dev merge=ours
# config.dev.json merge=ours

# 필요에 따라 추가 파일들을 아래에 나열하세요
# 예: *.local merge=ours
# 예: dev-only/ merge=ours

